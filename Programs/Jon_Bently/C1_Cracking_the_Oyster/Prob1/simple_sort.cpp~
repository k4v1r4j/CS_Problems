#include<iostream>
#include<set>
#include<fstream>
int main() {
  ifstream in("input");
  ofstream out("output");
  set<int>s;
  while(in>>t) {
    s.insert(t);
  }
  for(set<int>::iterator it=s.begin();it!=s.end();it++) {
    out<<*it;
  }
}
