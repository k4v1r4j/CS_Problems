#include<iostream>
#include<cstdlib>
#include<fstream>
using namespace std;

#define BITSPERINT 32
#define SHIFT 5
#define N 10000000
#define MASK 0x1F
int a[1+(N/(2*BITSPERINT))];

void bit_vector_mod(ifstream &in,ofstream &out) {
  int t;
  for(int i=0;i<N/2;i++) {
    clr(i);
  }
  while(cin>>t) {
    if(t<N/2)
      set(t);
  }
  for(int i=0;i<N/2;i++) {
    if(test(i))
      out<<i<<endl;
  }
}
